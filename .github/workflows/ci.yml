name: Continuous Integration

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  test-action:
    name: GitHub Actions Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        id: checkout
        uses: actions/checkout@v6

      - name: Test Local Action
        id: test-action-01
        uses: ./

      - name: Print summon version
        run: summon --version

      - name: Test Local Action
        id: test-action-02
        uses: ./

      - name: Print summon version
        run: summon --version
